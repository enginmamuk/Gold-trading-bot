import logging\nfrom datetime import datetime\nimport config\n\nlogger = logging.getLogger(__name__)\n\nclass StrategyAnalyzer:\n    \"\"\"Analyze signals and generate trading recommendations\"\"\"\n    \n    def __init__(self):\n        \"\"\"Initialize strategy analyzer\"\"\"\n        self.min_indicator_agreement = 0.5  # Minimum 50% of indicators must agree\n    \n    def generate_signal(self, indicators_data: dict, timeframe: str, current_price: float) -> dict:\n        \"\"\"\n        Generate trading signal based on indicators\n        \n        Args:\n            indicators_data: Technical indicators data\n            timeframe: Timeframe (5m, 15m, 1h)\n            current_price: Current price\n        \n        Returns:\n            Signal dictionary with entry, exit, risk management\n        \"\"\"\n        signal = {\n            'type': None,  # LONG, SHORT, or NEUTRAL\n            'timeframe': timeframe,\n            'price': current_price,\n            'timestamp': datetime.now().isoformat(),\n            'strength': 0,  # 0-100\n            'entry': None,\n            'stop_loss': None,\n            'take_profit': None,\n            'risk_reward': None,\n        }\n        \n        # Count bullish and bearish signals\n        bullish_count = 0\n        bearish_count = 0\n        total_indicators = 0\n        \n        # RSI Analysis\n        rsi_value = indicators_data.get('rsi', {}).get('value', 50)\n        if indicators_data.get('rsi', {}).get('oversold', False):\n            bullish_count += 1\n        elif indicators_data.get('rsi', {}).get('overbought', False):\n            bearish_count += 1\n        total_indicators += 1\n        \n        # MACD Analysis\n        if indicators_data.get('macd', {}).get('bullish_crossover', False):\n            bullish_count += 1\n        elif indicators_data.get('macd', {}).get('bearish_crossover', False):\n            bearish_count += 1\n        total_indicators += 1\n        \n        # Ichimoku Analysis\n        if indicators_data.get('ichimoku', {}).get('above_cloud', False):\n            bullish_count += 1\n        elif indicators_data.get('ichimoku', {}).get('below_cloud', False):\n            bearish_count += 1\n        total_indicators += 1\n        \n        # Bollinger Bands Analysis\n        if indicators_data.get('bollinger', {}).get('touch_lower', False):\n            bullish_count += 1\n        elif indicators_data.get('bollinger', {}).get('touch_upper', False):\n            bearish_count += 1\n        total_indicators += 1\n        \n        # Stochastic Analysis\n        if indicators_data.get('stochastic', {}).get('oversold', False):\n            bullish_count += 1\n        elif indicators_data.get('stochastic', {}).get('overbought', False):\n            bearish_count += 1\n        total_indicators += 1\n        \n        # Determine signal type\n        bullish_ratio = bullish_count / total_indicators if total_indicators > 0 else 0\n        bearish_ratio = bearish_count / total_indicators if total_indicators > 0 else 0\n        \n        if bullish_ratio > self.min_indicator_agreement:\n            signal['type'] = 'LONG'\n            signal['strength'] = int(bullish_ratio * 100)\n        elif bearish_ratio > self.min_indicator_agreement:\n            signal['type'] = 'SHORT'\n            signal['strength'] = int(bearish_ratio * 100)\n        else:\n            signal['type'] = 'NEUTRAL'\n            signal['strength'] = 0\n        \n        # Calculate entry, stop loss, and take profit\n        if signal['type'] in ['LONG', 'SHORT']:\n            atr_value = indicators_data.get('atr', {}).get('value', current_price * 0.01)\n            signal = self._calculate_risk_management(signal, current_price, atr_value)\n        \n        # Add indicator details\n        signal['rsi'] = rsi_value\n        signal['macd'] = 'BULLISH' if indicators_data.get('macd', {}).get('bullish_crossover') else 'BEARISH' if indicators_data.get('macd', {}).get('bearish_crossover') else 'NEUTRAL'\n        signal['ichimoku'] = 'ABOVE' if indicators_data.get('ichimoku', {}).get('above_cloud') else 'BELOW' if indicators_data.get('ichimoku', {}).get('below_cloud') else 'NEUTRAL'\n        signal['atr'] = f\"{atr_value:.2f}\"\n        \n        return signal\n    \n    def _calculate_risk_management(self, signal: dict, price: float, atr: float) -> dict:\n        \"\"\"\n        Calculate entry, stop loss, and take profit\n        \n        Args:\n            signal: Signal dictionary\n            price: Current price\n            atr: Average True Range value\n        \n        Returns:\n            Updated signal dictionary\n        \"\"\"\n        if signal['type'] == 'LONG':\n            # Entry at current price\n            signal['entry'] = price\n            \n            # Stop loss below current price (2 ATR)\n            signal['stop_loss'] = price - (atr * 2)\n            \n            # Take profit above current price (3 ATR = 3:1 risk/reward)\n            signal['take_profit'] = price + (atr * 6)  # 3x risk\n            \n        elif signal['type'] == 'SHORT':\n            # Entry at current price\n            signal['entry'] = price\n            \n            # Stop loss above current price (2 ATR)\n            signal['stop_loss'] = price + (atr * 2)\n            \n            # Take profit below current price (3 ATR = 3:1 risk/reward)\n            signal['take_profit'] = price - (atr * 6)  # 3x risk\n        \n        # Calculate risk/reward ratio\n        if signal['stop_loss'] and signal['take_profit']:\n            if signal['type'] == 'LONG':\n                risk = signal['entry'] - signal['stop_loss']\n                reward = signal['take_profit'] - signal['entry']\n            else:  # SHORT\n                risk = signal['stop_loss'] - signal['entry']\n                reward = signal['entry'] - signal['take_profit']\n            \n            if risk > 0:\n                signal['risk_reward'] = f\"{reward/risk:.2f}:1\"\n        \n        return signal\n    \n    def filter_by_news(self, signal: dict, news_filter: str) -> dict:\n        \"\"\"\n        Filter signal based on news events\n        \n        Args:\n            signal: Trading signal\n            news_filter: News filter strategy ('AVOID', 'REDUCE_SIZE', 'MONITOR', 'TRADE_NORMALLY')\n        \n        Returns:\n            Updated signal with news consideration\n        \"\"\"\n        signal['news_filter'] = news_filter\n        \n        if news_filter == 'AVOID':\n            signal['type'] = 'NEUTRAL'  # Don't trade during high-impact news\n            signal['reason'] = 'High-impact news event within 30 minutes'\n        elif news_filter == 'REDUCE_SIZE':\n            signal['reason'] = 'Medium-impact news event - reduce position size'\n        \n        return signal\n    \n    def multi_timeframe_analysis(self, signals: dict) -> dict:\n        \"\"\"\n        Analyze signals across multiple timeframes\n        \n        Args:\n            signals: Dictionary with signals for different timeframes\n                    {'5m': signal, '15m': signal, '1h': signal}\n        \n        Returns:\n            Multi-timeframe analysis result\n        \"\"\"\n        analysis = {\n            'overall_signal': 'NEUTRAL',\n            'confidence': 0,\n            'timeframes': signals,\n            'alignment': {},\n        }\n        \n        # Count signals\n        long_count = sum(1 for s in signals.values() if s.get('type') == 'LONG')\n        short_count = sum(1 for s in signals.values() if s.get('type') == 'SHORT')\n        \n        # Determine overall signal\n        if long_count > short_count and long_count > 0:\n            analysis['overall_signal'] = 'LONG'\n            analysis['confidence'] = int((long_count / len(signals)) * 100)\n        elif short_count > long_count and short_count > 0:\n            analysis['overall_signal'] = 'SHORT'\n            analysis['confidence'] = int((short_count / len(signals)) * 100)\n        \n        # Analyze alignment\n        for tf, signal in signals.items():\n            analysis['alignment'][tf] = signal.get('type', 'NEUTRAL')\n        \n        return analysis\n    \n    def get_signal_quality(self, signal: dict) -> str:\n        \"\"\"\n        Determine signal quality\n        \n        Args:\n            signal: Trading signal\n        \n        Returns:\n            Quality level ('STRONG', 'MEDIUM', 'WEAK', 'NEUTRAL')\n        \"\"\"\n        strength = signal.get('strength', 0)\n        \n        if signal['type'] == 'NEUTRAL':\n            return 'NEUTRAL'\n        elif strength >= 80:\n            return 'STRONG'\n        elif strength >= 60:\n            return 'MEDIUM'\n        else:\n            return 'WEAK'\n"